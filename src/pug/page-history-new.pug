extends layout/error.pug

block variables
  - const headerClass = ""
  - const bodyClass = "page-history preload hide-scrollbar"
  - const pageTitle = "История порта"
  - const siteLang = "ru"
  - const heading = "История порта"

block content
  include pages/history-new/main.pug
